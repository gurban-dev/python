'''
Concepts:
Arguments & parameters
Return types from functions
Scope
if-else statements


Exercise
Write a program that defines a function named compute_savings.

The function should accept two parameters: income and expenses.
Both parameters should have default values of None.


Program behavior:
If income is not provided when the function is called, prompt
the user to enter their monthly income.

If expenses is not provided, prompt the user to enter their
monthly expenses.

Calculate the monthly savings by subtracting expenses from income.

If the savings amount is less than 1, display a message stating
that the user did not save anything.

Otherwise, display how much money the user saved that month.

The function should return the calculated savings amount.


After defining the function:
Call the function without arguments so the user is prompted for input.

Store the returned value in a variable named savings and print it.

Call the function again with explicit arguments (income=100, expenses=50)
to demonstrate passing parameters directly.
'''