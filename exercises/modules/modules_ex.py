'''
Create a main.py program that imports variables defined inside
a module named capitals.py.

Only import two of the variables, and print each of them in the
main.py program.

Be sure to still import the module name after importing the two
variables.

For the variables that is not imported, use the module name
followed dot operator (.) followed by the variable named to
print it.

The capitals.py module should contain the following:
canada = "Ottawa"
france = "Paris"
greece = "Athens"
'''