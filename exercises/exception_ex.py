'''
Concept(s):
Exception handling with try / except.

1. Prompt the user to input a value.

2. Attempt to convert the input to an integer.

3. Catch a ValueError exception if the input cannot be
   converted to an integer.

A ValueError occurs when a function receives an argument
of the correct type but an invalid value (for example,
calling int() on a non-numeric string).
'''