"""
input() can only be passed to the int() and
float() functions if a numeric value is being
entered.

Inputting a value that cannot be converted to
an integer or float would generate an exception.

An exception is an unexpected error that occurs
while a program is running."""
given_name = float(input('Enter your given name: '))